namespace mvp.tickets.domain.Enums
{
    public enum ResponseCodes
    {
        Unknown = 0,
        Success = 200,
        BadRequest = 400,
        Unauthorized = 401,
        NotFound = 404,
        Error = 500,
    }
}
